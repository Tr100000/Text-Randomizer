### Changed
- Now uses Stonecutter for multi-version, multi-loader development
  - NeoForge is now supported!
- Reworked the config structure internally
- Improved the regex used to detect format placeholders in strings

### Fixed
- Fixed the config not saving default values when the file already exists
